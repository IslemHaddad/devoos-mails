# Jeton partage entre le master et les workers (bearer sur :8671).
# REMPLACE la valeur avant de committer, ou mieux : gere-le hors Git
# (sealed-secrets / SOPS / ExternalSecret) et retire ce fichier des resources.
apiVersion: v1
kind: Secret
metadata:
  name: devops-mails-agent
  labels:
    app: devops-mails
type: Opaque
stringData:
  token: "change-me-please"
